      
<%= form_for @project.prelationships.build(:pfollowed_id =>
                                           @project_id) do |f| %>
    <%= collection_select :prelationship, :pfollower_id, image_tag(Plant.image_path), :id,   
                          :name, {},:multiple => true,
                          :class => "listselect" %>
    <%= hidden_field_tag :project_id, @project.id %>
    <div class="actions">
      <%= f.submit "Pfollow" %>
    </div>
<% end %>  
<div class="circle">
    <div class="outer-circle"></div>
    <div class="middle-circle">
     <%= image_tag("beet.png", :mouseover => "onion.png" )%>
     <%= image_tag("onion.png")%>
      </div>
    <div class="inner-circle">Start With Veggies Here</div>
</div>

<div id="droppable1" class="droppablec">
	<p>Leftover Plants</p>
	<% @plants.each do |plant| 4.times do %>   
	<%= image_tag(plant.image_path, :class => "draggablea")%>     
	<% end %> 
	<% end %>                    
	</div>      

	    <script type="text/javascript">
	    $(function() {
	        $("#selectable").selectable({
	            stop: function(){
	                var result = $("#select-result").empty();
	                $(".ui-selected", this).each(function(){
	                    var index = $("#selectable li").index(this);
	                    result.append(" #" + (index + 1));
	                });
	            }
	        });
	    });
	    </script>


	<div class="demo">

	<p id="feedback">
	You've selected: <span id="select-result">none</span>.
	</p>

	<ul id="selectable">
	    Item 1
	    <li class="ui-widget-content">Item 2</li>
	    <li class="ui-widget-content">Item 3</li>
	    <li class="ui-widget-content">Item 4</li>
	    <li class="ui-widget-content">Item 5</li>
	    <li class="ui-widget-content">Item 6</li>
	</ul>
	
	